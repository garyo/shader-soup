# Anthropic API Key for LLM-based shader evolution
# API keys are now entered in the browser UI via the "API Key" button in the toolbar.
# Keys are stored in your browser's localStorage and never leave your machine.
# Get your API key from: https://console.anthropic.com/settings/keys

# Optional: You can still set this env var for local development (overrides UI key)
# VITE_ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Optional: Evolution Configuration
# VITE_EVOLUTION_MAX_DEBUG_ATTEMPTS=5
# VITE_EVOLUTION_DEFAULT_CHILDREN=10
# VITE_EVOLUTION_TIMEOUT_MS=30000
